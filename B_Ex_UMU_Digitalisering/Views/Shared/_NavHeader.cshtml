<header class="sticky-top">
    <nav class="navbar navbar-expand-lg navbar-dark bg-nav-color py-sm-0">
        <span class="navbar-brand d-lg-none">MENY</span>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#responsiveMenu" aria-controls="responsiveMenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="mdi mdi-menu"></span>
        </button>

        <div class="navbar-collapse collapse ml-auto" id="responsiveMenu">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="@Url.Page("/Index")" class="nav-link"
                       id="@(ViewBag.Current == "Home" ? "current" : "")"><i class="fas fa-home" aria-hidden="true"></i> HEM</a>
                </li>
                <li class="nav-item">
                    <a href="@Url.Page("/Index2")" class="nav-link"
                       id="@(ViewBag.Current == "Home2" ? "current" : "")"><i class="fab fa-accessible-icon" aria-hidden="true"></i> LÄNK 2</a>
                </li>
                <li class="nav-item">
                    <a href="@Url.Page("/Index3")" class="nav-link"
                       id="@(ViewBag.Current == "Home3" ? "current" : "")"><i class="fas fa-child" aria-hidden="true"></i> LÄNK 3</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="@Url.Page("/Account/Login")" class="nav-link last"
                       id="@(ViewBag.Current == "Login" ? "current" : "")"><i class="fas fa-user" aria-hidden="true"></i> LOGGA IN</a>
                </li>
            </ul>
        </div>
    </nav>
</header>
